# 01 · 总体架构：rsolver 是怎么工作的

> 阅读对象：想理解 `rsolver` 整体运作方式的人。
> 前置：无。本文所有名词都从零解释。

---

## 1. rsolver 是什么

`rsolver` 是《格里米斯的工匠》的 **Rust 求解器二进制**。它是一个**标准命令行程序**：

- **输入**：从标准输入（stdin）或命令行参数指定的文件读取一份**谜题 JSON**；
- **处理**：把 JSON 解析成内部模型，交给一串求解算法；
- **输出**：把结果序列化成**题解 JSON** 写到标准输出（stdout）。

```
$ rsolver puzzle.json > solution.json
$ cat puzzle.json | rsolver > solution.json
```

> Python 侧的 `src/solver/rust_solver.py` 就是起一个 `rsolver` 子进程、
> 喂 JSON、读回 JSON。所以「Rust 求解器」和外界唯一的边界就是**这份 JSON**。

---

## 2. 主流程：从 JSON 到 JSON

入口在 `src/main.rs`。全流程如下：

```
┌─────────────────────────────────────────────────────────────────────┐
│ main()                                                               │
│  1. 读入 JSON 文本（stdin 或参数文件）                                 │
│  2. serde_json 反序列化 → PuzzleJson（I/O 层模型）                      │
│  3. build_puzzle(&PuzzleJson) → Puzzle（内部领域模型）                  │
│        · 逐格建 Cell（含 number/symbol/compass/fence/shape_pattern）  │
│        · 逐边建 Edge（is_boundary / constraint）                      │
│        · 逐顶点建 Vertex（watchtower）                                │
│        · 收 shape_pool、rules、outer_boundaries                      │
│        · 任何坐标越界 / 边不合法 → 直接报错退出                          │
│  4. solver::solve(&puzzle, 30_000)  ← 30 秒超时预算                     │
│  5. solution_to_json(&Solution) → SolutionJson                       │
│  6. 写 stdout                                                         │
└─────────────────────────────────────────────────────────────────────┘
```

`PuzzleJson`（I/O 模型，`main.rs:22-107`）和 `Puzzle`（领域模型，`types.rs:150-167`）
是两套结构。前者完全贴合磁盘上的 JSON 形状（`edges[]` 是平铺列表、`grid{height,width}`
等），后者是为求解优化过的二维数组形态（`cells[r][c]`、`h_edges[r][c]`、`v_edges[r][c]`）。

### 2.1 一份输入 JSON 的骨架

```json
{
  "version": "1.0",
  "grid": { "height": 4, "width": 5 },
  "cells": [
    { "row": 0, "col": 0, "number": 3 },
    { "row": 0, "col": 1, "symbol": "A" },
    { "row": 1, "col": 2, "blocked": true },
    { "row": 2, "col": 3, "compass": { "up": 2, "down": 0, "left": null, "right": 1 } }
  ],
  "edges": [
    { "r1": 0, "c1": 0, "r2": 0, "c2": 1, "is_boundary": true },
    { "r1": 1, "c1": 2, "r2": 2, "c2": 2, "constraint": { "type": "difference", "value": 2 } }
  ],
  "vertices": [ { "row": 1, "col": 2, "watchtower": 3 } ],
  "outer_boundaries": [],
  "rules": [ { "type": "precise", "params": { "area": 3 } } ],
  "shape_pool": [ [[0,0],[1,0],[1,1]] ]
}
```

> 完整字段约定见 [02-数据结构](02-数据结构.md) 和 `main.rs` 的 `*Json` 结构体。

---

## 3. 核心：`solve()` 求解器路由

`src/solver/mod.rs:12` 的 `solve()` 是**大脑**。它按固定优先级尝试 4 个求解器：

```
solve(puzzle, timeout_ms)
│
├─ 特殊情况：无可填格 → 直接返回「已解决」（空区域集）
│
├─ ① AoG DFS ────────────────────────────────────────────────
│    ├─ 预算：rose 能力题 3s，否则给满 timeout_ms
│    ├─ 命中 → build_solution_trusted（内部校验权威，不再复查）
│    └─ 未命中且是纯 rose 题 → ② Rose（预算 min(剩余, 30s)）
│
├─ 路由判定：
│    ├─ 有 shape_pool / 面积数字线索 / 强罗盘线索
│    │      → ③ Pieces（DLX 精确覆盖）
│    └─ 否则 / ③ 失败 → ④ Backtrack（区域回溯）
│
└─ 全失败 → 返回 { solved:false, error_message:"No solution found" }
```

### 3.1 四段求解器的定位（一句话版）

| # | 求解器 | 一句话定位 | 擅长的规则 | 代码 |
|---|---|---|---|---|
| ① | **aog** | C++ 参考求解器的 1:1 移植，形状放置 + 海量剪枝 | 绝大多数官方题 | `solver/aog/*` |
| ② | **rose** | 专门对付 aog 超时的纯玫瑰窗题 | `rose_window` | `solver/rose/*` |
| ③ | **pieces** | 把「放形状」建模成精确覆盖，用 DLX 求解 | `shape_pool` / 面积数字 / 罗盘 | `solver/pieces.rs` |
| ④ | **backtrack** | 逐格归属的朴素回溯 | 通用兜底 | `solver/backtrack.rs` |

### 3.2 时间预算（重要细节）

`mod.rs:31-33` 的注释明确说明：**`timeout_ms` 是「每段求解器各自一份」的预算**，
而不是四段共享。也就是说：

- aog 拿到完整 `timeout_ms`（玫瑰窗题则缩到 `AOG_ROSE_BUDGET_MS = 3_000ms`，
  因为 aog 对无尺寸约束玫瑰窗题会长时间挂死）；
- rose 拿到 `min(剩余时间, ROSE_TIMEOUT_MS = 30_000ms)`；
- pieces / backtrack 又各自拿到 `timeout_ms`。

> 这解释了为什么 Python 侧要给子进程 3 倍的墙钟时间——因为子进程内部可能
> 依次花掉 30s + 30s + 30s。

### 3.3 判定公式

`solve()` 里用几个辅助函数决定走不走 pieces：

```
has_shape_pool   = !puzzle.shape_pool.is_empty()
has_area_clues   = 存在任意 cell.number（面积数字）
has_compass_clues= 存在「3 个方向以上」或「条带型」的罗盘
                  （strip = 左右都为0 或 上下都为0）

走 pieces ⟺ has_shape_pool || has_area_clues || has_compass_clues
否则/失败 → backtrack
```

`is_rose_capable` 判定是否给 rose 机会（`mod.rs:201-207`）：

```
有 rose_window 规则，且没有 same / different 规则  → true
```

---

## 4. 答案是怎么被构建和把关的

四个求解器解完都产出 `Vec<RegionInfo>`（区域列表）。`solve()` 调用**两种**答案构建器：

| 构建器 | 触发时机 | 行为 |
|---|---|---|
| `build_solution_trusted` | aog 命中 | 信任 aog 内部校验，直接把规则全标 true，**不再复查** |
| `build_solution` | rose / pieces / backtrack 命中 | ① 检查**不跨越预画边界**；② 跑 `constraints::check_all` 做规则复查；③ 有一条规则没过就标 unsolved |

```
build_solution(regions, start, puzzle)
│
├─ regions_respect_boundaries()？   ← 每段求解器产出都要过
│     「预画边界两侧必须是不同区域」，否则拒绝
│
├─ constraints::check_all()          ← 对每条规则跑 checker
│
└─ solved = 每条规则都通过
```

> **关键不变量**（CLAUDE.md 反复强调）：路由**独立复核**每个答案。aog 内部校验
> 被视为权威（信任），其余求解器的答案则必须过 `constraints.rs` + 边界尊重检查。
> 任何改动都不能破坏「错误答案无法通过校验链」。

### 4.1 完整的双重保险

```
                ┌─ aog 命中 → 内部校验权威（solver/validate.rs 仍在出口复核一次）
  4 个求解器 ──┤
                └─ rose/pieces/backtrack → 边界尊重 + constraints.rs 复查
                          │
                          ▼
                Python 侧 Router 再 IndependentValidator 全量复核（最终保险）
```

注意到 `aog/mod.rs:32-41`：aog 找到解后仍会调用 `validate::validate` 复核一次才返回，
所以「aog 信任」其实还有一层内层自检。

---

## 5. 模块依赖图

```
                        main.rs
                           │
   ┌──────────┬───────────┴──────────┬──────────────┐
   ▼          ▼                      ▼              ▼
types.rs   grid.rs           polyomino.rs    shapes.rs
 (模型)    (网格工具)          (多连块)    (形状/面积辅助)
   │          │                                constraints.rs
   └──────┬───┘                                (规则校验, 用 shapes::*)
          ▼
    solver/mod.rs  (solve 路由 + 答案构建)
       │    │    │    │
       ▼    ▼    ▼    ▼
     aog  rose pieces backtrack
     │            │    （都用 types::*、grid::*、polyomino::*、shapes::*）
     │            └──► dlx.rs (Dancing Links)
     ▼
 solver/validate.rs  （独立验证器，aog 出口与 rose 验收共用）
```

依赖方向大致是：

```
main.rs → solver/mod.rs → {aog, rose, pieces, backtrack}
每个子求解器 → types.rs / grid.rs / polyomino.rs / shapes.rs / dlx.rs
solver/mod.rs → constraints.rs（复查；constraints → shapes::*）
solver/validate.rs ← 被 aog/mod.rs 与 rose/mod.rs 共用（validate → shapes::*）
```

### 5.1 每个文件一句话

| 文件 | 行数 | 作用 |
|---|---|---|
| `main.rs` | 327 | 入口、JSON 模型、`build_puzzle`、输出序列化 |
| `types.rs` | 221 | 领域模型与共用工具 |
| `grid.rs` | 74 | `is_adjacent_free` / `fillable_cells` |
| `shapes.rs` | 184 | 形状/面积共享辅助：`dihedral_key` / `is_rectangle` / `collect_pool_shapes` / `area_bounds` / `rose_symbol_types` |
| `dlx.rs` | 270 | 通用 Dancing Links 精确覆盖引擎 |
| `polyomino.rs` | 74 | 多连块的 8 种变换与自由多连块生成 |
| `constraints.rs` | 336 | 规则 checker（部分 stub，复用 `shapes::*`） |
| `solver/mod.rs` | 239 | 路由 `solve()`、答案构建、边界尊重 |
| `solver/aog/*` | ~2960 | AoG DFS 全套 |
| `solver/validate.rs` | 635 | 完整独立验证器（aog 出口 + rose 验收共用） |
| `solver/pieces.rs` | 634 | DLX 精确覆盖求解器 |
| `solver/backtrack.rs` | 801 | 区域回溯 |
| `solver/rose/*` | ~1490 | 玫瑰窗求解器 |

---

## 6. 数据流：一个具体谜题走完全程

以一份 5×4、形状池规则的题（官方 0095）为例：

```
① main.rs 读 JSON ─► PuzzleJson（平铺数组）
② build_puzzle ────► Puzzle { cells[4][5], h_edges[4][4], v_edges[3][5], ... }
③ solve() ─────────► aog 构建 AoGCore（把 Puzzle 编码进 (2h+5)×(2w+5) 位域网格）
④ aog dfs() ───────► 找到解 → extract_regions → Vec<RegionInfo>
⑤ validate 复核 ───► 通过
⑥ build_solution_trusted → SolutionJson（regions + rule_results 全 true）
⑦ stdout ──────────► 题解 JSON
```

题解 JSON 形如：

```json
{
  "solved": true,
  "steps_taken": 0,
  "elapsed_ms": 42,
  "error_message": null,
  "regions": [
    { "region_id": 1, "cells": [[0,0],[1,0],[2,0]], "area": 3,
      "shape": [[0,0],[1,0],[2,0]],
      "normalized_shape_key": "(0,0)(1,0)(2,0)", "matched_shape_name": null }
  ],
  "rule_results": { "shape_pool": true }
}
```

> `steps_taken` 在 Rust 侧恒为 0（Python 才统计步数）。`elapsed_ms` 是内部耗时。
> `rule_results` 对 aog 命中是全 true，对其它求解器是 `constraints.rs` 的复查结果。

---

## 7. 关键设计决策备忘

1. **aog 是 1:1 移植**：`aog/` 每个函数几乎都能在 `third_party/AoG_Solver` 的 C++
   源码里找到对应。目的是**行为完全对齐**，官方题结论可交叉验证。
2. **rose 单独成模块**：aog 移植得很忠诚，反而暴露了它对无尺寸约束玫瑰窗题的
   短板，所以单独写了 rose 求解器来兜。
3. **预画边界是全求解器硬约束**：aog 把它编码成 `LINE_BLOCK`；pieces/backtrack
   不感知边界，所以 `build_solution` 里的 `regions_respect_boundaries` 是**最后一道闸**。
4. **答案可被拒绝**：一个求解器「找到解」≠ 接受——要过验证链才算数，否则路由
   继续尝试下一个求解器。

---

## 8. 本节代码索引

| 主题 | 位置 |
|---|---|
| `main()` 入口 | `main.rs:284` |
| JSON 反序列化模型 `PuzzleJson` | `main.rs:22-107` |
| `build_puzzle` 建内部模型 | `main.rs:138-260` |
| 路由 `solve()` | `solver/mod.rs:12` |
| rose 能力判定 `is_rose_capable` | `solver/mod.rs:201` |
| 时间常量 | `solver/mod.rs:209-210` |
| `build_solution`（复查） | `solver/mod.rs:161` |
| `build_solution_trusted`（信任） | `solver/mod.rs:181` |
| 边界尊重 `regions_respect_boundaries` | `solver/mod.rs:125` |
| aog 出口复核 `validate` | `solver/aog/mod.rs:34` |

---

下一节：[02-数据结构](02-数据结构.md)
